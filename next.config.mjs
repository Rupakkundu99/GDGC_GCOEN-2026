/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        unoptimized: true,
        domains: ['gdgc-gcoen.vercel.app','cloud.appwrite.io'],
      },
};

export default nextConfig;
